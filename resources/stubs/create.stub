@extends('admin.layouts.app')

@section('content')
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h5 class="mb-0 text-gray-800 fw-bold">Create {{ modelName }}</h5>
        <a href="{{ route('admin.{{ modelVariablePlural }}.index') }}" class="btn btn-secondary btn-sm">
            <i class="bi bi-arrow-left me-1"></i> Back
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <form action="{{ route('admin.{{ modelVariablePlural }}.store') }}" method="POST" {{ enctype }}>
                @csrf
                <div class="row">
{{ formFields }}
                </div>
                
                <div class="mt-4 d-flex justify-content-end gap-2">
                    <a href="{{ route('admin.{{ modelVariablePlural }}.index') }}" class="btn btn-light">Cancel</a>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save me-1"></i> Save {{ modelName }}
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
@endsection
